const person = ["John", 30, {address:"Kathmandu"}];

// During variable assigning, value are stored in variable .
const Name = person[0];
const Address = person[2];
const AddressValue = person[2].address;
console.log(Name)
console.log(Address)
console.log(AddressValue)

